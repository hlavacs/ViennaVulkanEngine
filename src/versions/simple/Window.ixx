/**
 * @file Window.ixx
 * @brief Declares the simple engine Window module scaffold for the facade role that reports window information such as number, renderer, camera, and size; the WindowSystem implementation owns Window implementations and returns Window wrappers. Conceptual counterpart: src/versions/v5/Window.ixx.
 * @todo Define the minimal simple-engine Window facade contract when the subsystem is implemented.
 */
export module VEEngine.Simple.Window;
