/**
 * @file GUI.ixx
 * @brief Declares the simple engine GUI facade scaffold, conceptually matching the v5 GUI counterpart.
 *
 * The GUI facade wraps the GUI system implementation, offers public hooks for creating widgets, and must work with ImGui.
 *
 * @todo Define the minimal simple engine GUI facade contract once the implementation boundary is specified.
 */
export module VEEngine.Simple.GUI;
