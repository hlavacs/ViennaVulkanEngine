export module VEEngine;
export import :Error;
export import :Math;
export import :Types;
export import :ECS;
export import :Window;
export import :World;

/// @file
/// @brief Minimal public root module for shared math, ECS, window, and world facade types.
